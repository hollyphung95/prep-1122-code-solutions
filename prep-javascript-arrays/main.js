/* Create an array with six string values */
/* Assign it to a variable named languages */
const languages = ['html', 'css', 'javascript', 'react', 'node', 'postgres'];

/* console log the languages variable and confirm that it matches the output */
console.log(languages);

/* Declare a new variable named firstElement */
/* Assign it the value of the languages variable at the first index */
var firstElement = languages[0];

/* console log the value of firstElement variable and confirm that it matches the output */
console.log('The first element of the array is', firstElement);

/* Declare a new variable named thirdElement */
/* Assign it the value of the languages variable at the third index */
var thirdElement = languages[2];

/* console log the value of thirdElement variable and confirm that it matches the output */
console.log('The third element of the array is', thirdElement);

/* Declare a new variable named length */
/* Assign it the value of the length of the languages array */
var length = languages.length;

/* console log the value of length variable and confirm that it matches the output */
console.log('The length of the array is', length);

/* Declare a new variable named lastElement */
/* Assign it the value of the languages variable at the last index */
var lastElement = languages[languages.length - 1];

/* console log the value of lastElement variable and confirm that it matches the output */
console.log('The last element of the array is', lastElement);
